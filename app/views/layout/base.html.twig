<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title|default('Portal RRHH') }}</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
    <div class="sidebar">
        <div class="logo-container">
            <img src="/assets/images/mihospital_logo.png" alt="Logo Aplicación">
        </div>
        <nav class="sidebar-nav">
            <ul>
                {# The 'app.request.query.get('page')' is a common way to get GET params in frameworks #}
                {# We will need to pass the 'page' variable to the template from the controller #}
                <li><a href="/?page=dashboard_main" class="{{ page == 'dashboard_main' ? 'active' : '' }}"><i class="fas fa-home"></i> Menú Principal</a></li>

                {% if 'user_roles' in session and session.user_roles is not empty %}
                    <li><a href="/?page=fichadas&tarjeta={{ session.user_tarjeta|url_encode }}" class="{{ page == 'fichadas' ? 'active' : '' }}"><i class="fas fa-calendar-check"></i> Mis Fichadas</a></li>
                    <li><a href="/?page=cambiar_contrasena" class="{{ page == 'cambiar_contrasena' ? 'active' : '' }}"><i class="fas fa-key"></i> Cambiar Contraseña</a></li>
                {% endif %}

                {% if 'jefe' in session.user_roles %}
                    <li><a href="/?page=dashboard_jefe" class="{{ page == 'dashboard_jefe' ? 'active' : '' }}"><i class="fas fa-user-tie"></i> Panel de Jefe</a></li>
                {% endif %}
                {% if 'supervisor' in session.user_roles %}
                    <li><a href="/?page=dashboard_supervisor" class="{{ page == 'dashboard_supervisor' ? 'active' : '' }}"><i class="fas fa-user-graduate"></i> Panel de Supervisor</a></li>
                {% endif %}
                {% if 'rrhh' in session.user_roles %}
                    <li><a href="/?page=dashboard_rrhh" class="{{ page == 'dashboard_rrhh' ? 'active' : '' }}"><i class="fas fa-users-cog"></i> Panel de RRHH</a></li>
                {% endif %}
                {% if 'admin' in session.user_roles %}
                    <li><a href="/?page=dashboard_admin" class="{{ ['dashboard_admin', 'gestion_usuarios'] in page ? 'active' : '' }}"><i class="fas fa-shield-alt"></i> Panel de Admin</a></li>
                {% endif %}

                <li><a href="/?page=logout"><i class="fas fa-sign-out-alt"></i> Cerrar Sesión</a></li>
            </ul>
        </nav>
    </div>
    <div class="main-wrapper">
        <div class="top-navbar">
        <div class="content-area">
            {% block content %}{% endblock %}
        </div>
        </div>
    </div>
    <footer class="footer">
    </footer>
</body>
</html>